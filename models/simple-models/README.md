# Simple Models

- These models are not representative of an average Alloy Model, and exist to test specific macros and features of the translator. 

- Each model is complex enough to test just one aspect of the language.

- These tests are meant to check the correctness of the translator, and are run with very small scopes. None of these are supposed to time out